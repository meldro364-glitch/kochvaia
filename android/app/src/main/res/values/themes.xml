<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Compose owns colors; we just need a Material-Components-compatible base
         so the Activity inflation works before setContent. -->
    <style name="Theme.Kochvaia" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
